<Context>
    <!-- Ehcache configuration falls back to classpath resource /ehcache.xml
    <Environment name="cacheConfig" value="ehcache.xml" type="java.lang.String"/>
    -->
    <!-- Application configuration falls back to classpath resource /be/vliz/emodnet/querytool/main/prod.properties -->
    <Environment name="appConfig" value="prod.properties" type="java.lang.String"/>
</Context>